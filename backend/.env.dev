# Development Environment Configuration
# This file is used for local development in Docker containers

# Application Configuration
APP_NAME=Server Building Dashboard (DEV)
ENVIRONMENT=development
DEBUG=true

# Security - Simple secret for dev (NEVER use in production)
SECRET_KEY=dev-secret-key-not-for-production-use-only

# Session Configuration
SESSION_LIFETIME_SECONDS=86400  # 24 hours for dev convenience
COOKIE_DOMAIN=

# CORS Origins (comma-separated) - Allow common dev ports
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8080

# Frontend URL
FRONTEND_URL=http://localhost:5173

# SAML2 Configuration (required fields but can use placeholder values in dev)
# For actual SAML testing, replace with real values and add idp_metadata.xml
SAML_METADATA_PATH=./saml_metadata/idp_metadata.xml
SAML_ENTITY_ID=http://localhost:8000
SAML_ACS_URL=http://localhost:8000/auth/callback
SAML_SLS_URL=

# Rate Limiting (more relaxed for development)
RATE_LIMIT_PER_MINUTE=1000
RATE_LIMIT_BURST=2000

# Logging (more verbose in dev)
LOG_LEVEL=DEBUG

# Database
DATABASE_URL=
